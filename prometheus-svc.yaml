apiVersion: v1
kind: Service
metadata:
  name: prometheus-service
  namespace: monitoring
spec:
  selector:
    app: prometheus # Routes traffic to prometheus pod
  ports:
    - name: http
      protocol: TCP
      port: 9090  # Cluster internal service port
      targetPort: 9090
  type: NodePort
